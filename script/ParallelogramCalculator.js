function ParallelogramCalculator(){
    const base = getElementValue('wideValue');
}
function getElementValue (element){
    const inputField = document.getElementById(element);
    const inputVal= inputField.value ;
    console.log(inputVal);
}